[package]
name = "test_utilities"
version = "0.1.0"
edition = "2024"

[dependencies]
config.workspace = true
directories.workspace = true

erp = { path = ".." }
code_gen = { path = "../../code_gen" }
